export interface IPortfolio {
  id: string;
  price: string;
  count: string;
}

export interface IPortfolioTableItem {
  id: string;
  name: string;
  purchasePrice: string;
  currentPrice: string;
  count: string;
  index: number;
}
